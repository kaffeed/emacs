;;; lsp-config.el --- Deprecated: migrated to eglot-config.el -*- lexical-binding: t -*-
;;
;; This file is kept for reference only.
;; LSP configuration has been migrated from lsp-mode to eglot.
;; The active configuration is in eglot-config.el.
;;
;; See eglot-config.el for:
;;   - eglot hooks (replaces lsp-deferred hooks)
;;   - YAML schema configuration (replaces lsp-yaml)
;;   - dape debug templates (replaces dap-mode/dap-netcore/dap-go/dap-node)

(message "lsp-config.el is deprecated; eglot-config.el is now loaded instead.")

(provide 'lsp-config)
;;; lsp-config.el ends here
